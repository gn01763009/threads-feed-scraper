{
    "title": "Threads Custom Feed Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "feedUrls": {
            "title": "Feed URLs",
            "type": "array",
            "description": "One or more Threads custom feed URLs (e.g. https://www.threads.com/custom_feed/18113589370710265)",
            "editor": "stringList",
            "items": {
                "type": "string"
            }
        },
        "maxPosts": {
            "title": "Max Posts",
            "type": "integer",
            "description": "Maximum number of posts to scrape per feed",
            "default": 50,
            "minimum": 1,
            "maximum": 200
        },
        "scrollCount": {
            "title": "Scroll Count",
            "type": "integer",
            "description": "Number of times to scroll down to load more posts",
            "default": 5,
            "minimum": 1,
            "maximum": 20
        }
    },
    "required": ["feedUrls"]
}
